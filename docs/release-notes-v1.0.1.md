# DEEPDSP-AMC v1.0.1

This patch release corrects the right-hand label layout in the repository banner.

## Visual correction

- Centers all three architecture labels within their cards.
- Reduces the label size from 18 px to 16 px to preserve safe horizontal padding under GitHub font fallbacks.
- Keeps the existing restrained color animation and ASCII-only English text.
- Adds a repository validation check for label centering and minimum horizontal padding.

## Academic artifacts

The report, executed notebook, trained models, evaluation results, demo app, and video are unchanged from v1.0.0. The attached artifacts remain the complete audited project package.
